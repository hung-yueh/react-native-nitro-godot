diff --git a/platform/ios/SCsub b/platform/ios/SCsub
index a313442..b158011 100644
--- a/platform/ios/SCsub
+++ b/platform/ios/SCsub
@@ -16,6 +16,10 @@ ios_lib = [
     "os_ios.mm",
 ]
 
+# libgodot C-API entry point for library builds (patched by build_godot.sh)
+if env["library_type"] != "executable":
+    ios_lib.append("libgodot_ios.mm")
+
 env_ios = env.Clone()
 ios_lib = env_ios.add_library("ios", ios_lib)
 
diff --git a/platform/ios/libgodot_ios.mm b/platform/ios/libgodot_ios.mm
new file mode 100644
index 0000000..a0f4760
--- /dev/null
+++ b/platform/ios/libgodot_ios.mm
@@ -0,0 +1,39 @@
+// libgodot_ios.cpp — C-API entry point for iOS library builds.
+// Generated by build_godot.sh (not present in upstream Godot 4.7-dev2).
+
+#include "core/extension/libgodot.h"
+#include "core/extension/godot_instance.h"
+#include "main/main.h"
+#include "os_ios.h"
+
+static OS_IOS *os = nullptr;
+static GodotInstance *instance = nullptr;
+
+GDExtensionObjectPtr libgodot_create_godot_instance(int p_argc, char *p_argv[], GDExtensionInitializationFunction p_init_func) {
+	ERR_FAIL_COND_V_MSG(instance != nullptr, nullptr, "Only one Godot Instance may be created.");
+
+	os = new OS_IOS();
+
+	Error err = Main::setup(p_argv[0], p_argc - 1, &p_argv[1], false);
+	if (err != OK) {
+		return nullptr;
+	}
+
+	instance = memnew(GodotInstance);
+	if (!instance->initialize(p_init_func)) {
+		memdelete(instance);
+		return nullptr;
+	}
+
+	return (GDExtensionObjectPtr)instance;
+}
+
+void libgodot_destroy_godot_instance(GDExtensionObjectPtr p_godot_instance) {
+	GodotInstance *godot_instance = (GodotInstance *)p_godot_instance;
+	if (instance == godot_instance) {
+		godot_instance->stop();
+		memdelete(godot_instance);
+		instance = nullptr;
+		Main::cleanup();
+	}
+}
