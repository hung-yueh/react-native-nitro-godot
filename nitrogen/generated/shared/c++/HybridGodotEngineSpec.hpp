///
/// HybridGodotEngineSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `FrameStats` to properly resolve imports.
namespace margelo::nitro::godot { struct FrameStats; }

#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <functional>
#include <vector>
#include <optional>
#include "FrameStats.hpp"

namespace margelo::nitro::godot {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `GodotEngine`
   * Inherit this class to create instances of `HybridGodotEngineSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridGodotEngine: public HybridGodotEngineSpec {
   * public:
   *   HybridGodotEngine(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridGodotEngineSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridGodotEngineSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridGodotEngineSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void initialize(const std::string& pckPath) = 0;
      virtual void start() = 0;
      virtual void pause() = 0;
      virtual void destroy() = 0;
      virtual void attachSurface(uint64_t surfacePointer) = 0;
      virtual void updateSharedBuffer(const std::shared_ptr<ArrayBuffer>& buffer) = 0;
      virtual void sendMessage(const std::string& message) = 0;
      virtual std::string pollMessage() = 0;
      virtual void notifyPollingStopped() = 0;
      virtual void setOnWakeUp(const std::function<void()>& callback) = 0;
      virtual void suspendOS() = 0;
      virtual void resumeOS(uint64_t newSurfacePointer) = 0;
      virtual void loadSceneAsync(const std::string& scenePckPath) = 0;
      virtual std::optional<std::vector<double>> unprojectPosition(double x, double y, double z) = 0;
      virtual void sendTouchEvent(double x, double y, bool pressed, double index) = 0;
      virtual void sendDragEvent(double x, double y, double relativeX, double relativeY, double velocityX, double velocityY, double index) = 0;
      virtual void resizeSurface(double width, double height) = 0;
      virtual std::string getLastError() = 0;
      virtual FrameStats getFrameStats() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "GodotEngine";
  };

} // namespace margelo::nitro::godot
